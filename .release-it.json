{
  "github": {
    "release": true
  },
  "git": {
    "changelog": "npx auto-changelog --commit-limit false --stdout --unreleased --template changelog.format.hbs"
  },
  "hooks": {
    "before:init": ["npm run compile", "npm run lint", "npm test"],
    "after:bump": "npx auto-changelog -p"
  }
}
